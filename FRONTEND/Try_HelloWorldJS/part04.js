// isBigger() 함수의 내용을 수정해서 a가 b보다 크면 true, 그렇지 않으면 false로 반환하도록
function isBigger(a, b) {
  // 아래 줄을 수정해서 a가 b보다 큰지 아닌지를 반환하도록 만들기

  return a > b;
}

// is BothTrue() 함수의 내용을 수정해서 a와 b가 모두 true면 true, 그렇지 않으면 false를 반환하도록 만들기 && 연산자를 이용해야함
function isBothTrue(a, b) {
  return a && b;
}
